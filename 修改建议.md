

具体我会分 4 层改，不是只改 `db.py`。

**1. 先改 [db.py](C:/VerbalVis2/backend/db.py)**

在 `fact_order` 前面新增两个汇总表：

```sql
CREATE TABLE item_summary AS ...
CREATE TABLE payment_summary AS ...
```

`item_summary` 负责每个订单的商品/运费/卖家信息：

```text
item_count
product_count
category_count
seller_count
order_item_revenue
freight_total
avg_item_price
freight_ratio
```

`payment_summary` 负责每个订单的支付信息：

```text
primary_payment_type
payment_method_count
max_payment_installments
```

然后扩展 `fact_order`：

```text
estimated_delivery_days
delivery_delay_days
is_late
delivery_status_bucket
delay_bucket
review_bucket
is_high_score
default_is_low_score
revenue_bucket
freight_bucket
order_size_bucket
```

注意：我会把低分字段叫 `default_is_low_score`，表示默认 `review_score <= 2`。真正动态低分还是让 `tools.py` 按用户设置的阈值算。

**2. 同步字段注册**

`db.py` 里的 `FIELDS` 要加入这些新字段：

```text
delivery_status_bucket
delay_bucket
review_bucket
default_is_low_score
is_high_score
item_count
seller_count
category_count
freight_total
freight_ratio
primary_payment_type
payment_method_count
max_payment_installments
revenue_bucket
freight_bucket
order_size_bucket
```

`_FIELD_COL` 里 `fact_order` 和 `fact_item` 都要映射。  
`fact_item` 也要把这些订单级字段从 `fact_order` 带过去，这样按品类筛选/分组时也能用。

**3. 改 [tools.py](C:/VerbalVis2/backend/tools.py)**

新增可聚合指标：

```text
late_ratio
on_time_ratio
high_score_ratio
avg_freight_ratio
```

这样用户可以说：

```text
延迟率最高的 Top 10 品类
准时订单占比饼图
高评分占比最高的州
运费占比最高的品类
```

工具层会自动算比例，不需要模型临时拼逻辑。

**4. 改提示词和前端标签**

[prompts.py](C:/VerbalVis2/backend/prompts.py) 里加自然语言映射：

```text
延迟/超时 = is_late 或 delivery_status_bucket
准时 = delivery_status_bucket = on_time
低分订单 = low_score_ratio 或 default_is_low_score
高分订单 = is_high_score
大订单 = order_size_bucket
运费高 = freight_bucket 或 freight_total
支付方式 = primary_payment_type
```

前端字段名也要加中文显示：

```text
delivery_status_bucket -> 配送状态
delay_bucket -> 延迟程度
review_bucket -> 评分分组
freight_ratio -> 运费占比
primary_payment_type -> 支付方式
order_size_bucket -> 订单规模
```

我建议按这个顺序改：**先 `db.py`，再 `tools.py`，再提示词，最后前端显示名**。这样字段从数据层一路通到界面，不会中间断掉。我还没完成全部修改，中途被工具额度限制挡住了。现在状态是：`db.py` 已经改了一部分，新增了订单商品汇总、支付汇总、配送延迟、评分分组、订单规模、运费、支付方式这些字段；但我刚准备继续改 `tools.py` 时编辑工具被拒绝了。

重要提醒：当前代码处在**半完成状态**，我已经改到 `tools.py` 的颜色字段 schema，但还没来得及把常量位置调整好。也就是说现在需要继续修完，否则可能会有导入错误。

我下一步需要继续完成：

- 修正 `tools.py` 常量定义位置
- 加上 `late_ratio / on_time_ratio / high_score_ratio / avg_freight_ratio`
- 同步提示词和 Qwen 规则
- 同步前端字段中文名
- 跑 `py_compile` 和工具级回归测试

现在不是逻辑问题，是本轮编辑工具被限制了。你可以稍后让我继续，我会从这个半完成点接着修，不会从头来。我同意，而且我看了原始表头后更确定：`db.py` 现在应该承担更多“业务口径预计算”。

但我会稍微修正一处：**不要把 `is_low_score` 作为唯一真理硬编码死**，因为我们现在已经支持用户动态说“低分改成 3 分及以下”。所以可以有 `review_bucket`，但 `低分占比` 这种最好继续由 `tools.py` 根据当前阈值算。

我建议这样落地：

**应该加进 `db.py` 的字段**

1. 配送字段，强烈建议加：
```text
estimated_delivery_days
delivery_delay_days
is_late
delivery_status_bucket
delay_bucket
```

原始表里有 `order_estimated_delivery_date`，所以这个很稳。之后用户说“延迟订单”“准时订单”“延迟最多的品类”“准时率饼图”，系统就不用猜。

2. 评分分组，可以加：
```text
review_bucket
is_high_score
```

`is_low_score` 我建议谨慎。可以叫 `is_default_low_score`，表示默认 `review_score <= 2`，但真正的低分口径还是用当前全局阈值。

3. 订单规模字段，应该加：
```text
item_count
product_count
category_count
seller_count
order_item_revenue
freight_total
avg_item_price
freight_ratio
order_size_bucket
```

这能支持“大订单”“多商品订单”“运费占比高”“单卖家/多卖家订单”等自然语言。

4. 支付字段，应该加：
```text
primary_payment_type
payment_method_count
max_payment_installments
```

我不建议叫 `payment_installments`，因为一个订单可能多条 payment。用 `max_payment_installments` 或 `primary_payment_installments` 口径更稳。

5. 语音友好的分桶字段，应该加：
```text
revenue_bucket
freight_bucket
order_size_bucket
delay_bucket
delivery_status_bucket
review_bucket
```

这些对“饼图/构成/占比”很关键。

**还要配套改的地方**

只改 `db.py` 不够，还要同步：

- [tools.py](C:/VerbalVis2/backend/tools.py)：把新字段加入可筛选、可分组、可排序；新增 `late_ratio` 这类派生指标。
- [prompts.py](C:/VerbalVis2/backend/prompts.py)：告诉模型“延迟=delivery_status_bucket 或 is_late”，“大订单=order_size_bucket”。
- [realtime_qwen.py](C:/VerbalVis2/backend/realtime_qwen.py)：同步 Qwen 高优先级规则。
- 前端字段名映射：图表轴、tooltip、筛选标签里显示中文，不要显示生硬字段名。

**我最终建议的版本**

`db.py` 负责生成稳定业务字段；  
`tools.py` 负责动态指标，比如：

```text
low_score_ratio
late_ratio
on_time_ratio
high_score_ratio
avg_freight_ratio
```

这样最干净。`db.py` 不要直接把所有原始列暴露出去，而是做成一套“语音分析友好”的事实表。这个方向我完全同意，值得马上改。