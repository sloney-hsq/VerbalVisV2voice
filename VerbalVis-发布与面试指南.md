# VerbalVis：GitHub 发布与面试指南

> 适用版本：本仓库当前的 VerbalVis FD-Voice 运行时实现。本文用于 GitHub 发布、简历投递和面试准备。所有表述以代码、运行时契约和测试为准；**不把研究原型包装为生产级 BI 平台，也不把独立 DataOps Agent 表述为已接入 VerbalVis 的能力。**

## 目录

- [一、项目定位与可信边界](#一项目定位与可信边界)
- [二、GitHub 发布说明](#二github-发布说明)
- [三、中文简历项目表述](#三中文简历项目表述)
- [四、100 道高频面试问答](#四100-道高频面试问答)

## 一、项目定位与可信边界

### 1. 一句话定位

**VerbalVis FD-Voice 是一个面向 Olist 电商数据的单参与者、全双工语音驱动可视分析原型：它把实时语音、结构化工具调用、DuckDB 分析和共享仪表盘组织为可中断、可追踪、可条件提交的响应事务。**

项目并不是“把语音转成 SQL”这么简单。真正要处理的问题是：用户可能在模型播报或工具执行时插话；这句话可能是附和、识别修正、停止，也可能是新的分析目标。系统需要避免两类常见错误：把“嗯，继续”误杀为取消，或让已经过期的工具结果覆盖用户刚刚修改后的仪表盘。

### 2. 当前已实现的核心能力

- 浏览器音频经 WebSocket 接入 Qwen-Omni-Realtime；后端保持一个实时会话，前端以 Vue 3、Pinia 和 Vega-Lite 展示共享仪表盘。
- 将模型可调用能力收敛为 **11 个 schema-grounded 分析工具**，由 DuckDB 对 Olist 数据进行确定性聚合、筛选和图表操作。
- 每个模型响应携带不可变三元组：`(response_id, intent_epoch, base_revision)`；运行时有 `STREAMING`、`OVERLAP_PENDING`、`PROPOSING`、`EXECUTING_DRAFT`、`COMMITTED`、`SUPERSEDED`、`CANCELLED`、`FAILED` 八种状态。
- `speech_started` 仅表示语音重叠，而不是立即取消；最终转写后将打断保守区分为 `BACKCHANNEL`、`RECOGNITION_REPAIR`、`STOP_ONLY` 和 `ANALYTICAL_REVISION`。
- 工具调用先在私有 `DashboardDraft` 中顺序运行，随后以 intent epoch、dashboard revision、事务状态和取消标志进行条件提交；过期草稿会被 `stale_discarded`，不会进入浏览器、模型上下文或公开仪表盘。
- 所有工具契约都绑定既有的 Qwen `TOOL_SCHEMAS` 单一来源，进一步说明读/写模式、依赖、前置条件、幂等性、可取消性和副作用。
- WebSocket、事务和前端都记录/处理 `response_overlap`、`response_resumed`、`response_superseded`、`dashboard_commit`、`tool_batch_finished` 等事件；运行时输出 JSONL trace，便于复盘一次响应为什么提交、失败或被丢弃。

### 3. 必须主动说明的边界

1. **单参与者、内存态原型。** 当前仪表盘状态、undo 历史和响应协调都在进程内；第二个浏览器会话会被拒绝。它不是 Redis 支撑的多进程、多租户协作系统。
2. **逻辑失效不等于 CPU 级抢占。** 历史同步 Python 工具处理器声明为不可抢占；当用户改变意图后，它可能物理执行完，但其结果会被丢弃，不能污染可见状态或后续模型推理。
3. **不是任意 SQL Agent。** 模型只能调用受 schema 约束的分析工具，不能生成或执行任意代码；这是为保证 Olist 指标口径、可控图表操作和可测试状态变更而作出的选择。
4. **没有用户效能的因果性结论。** 当前证据重点是机制、运行时 trace、回归测试和案例式演示；不能写“已证明显著提高效率/可用性”。
5. **DataOps Agent 是独立模块。** 仓库中的 `dataops_agent` 有其自己的接入、质量审计、SQL、MCP 与可选 Redis/Elasticsearch 路径，但尚未被 `backend/main.py`、`backend/realtime.py`、`backend/tools.py` 或响应协调器接入。

更多协议细节见：[响应事务合同](docs/verbalvis-runtime-contract.md)；安装、配置和工具清单见：[README](README.md)。

## 二、GitHub 发布说明

### 1. 推荐的仓库首页摘要

可以将下面这段作为 README 的开头或 GitHub About 的基础：

> VerbalVis FD-Voice is a single-participant, full-duplex voice-driven visual analytics prototype for the Olist e-commerce dataset. It coordinates realtime voice interaction, schema-grounded analytical tools, DuckDB queries, and a shared Vega-Lite dashboard through a response-transaction runtime. Speech overlap is not treated as an automatic cancellation: the system waits for final transcription, classifies the interruption intent, executes tool calls in a private dashboard draft, and conditionally commits only results that are still semantically current.

中文版本：

> VerbalVis 是一个面向 Olist 电商数据的全双工语音可视分析原型。系统把实时语音、受 schema 约束的分析工具、DuckDB 聚合与共享 Vega-Lite 仪表盘纳入响应事务：用户开口先被视为重叠信号，最终转写后再判断是否真的修订分析意图；工具结果先写入私有草稿，只有仍符合当前意图和仪表盘版本时才原子提交。

### 2. 发布前检查清单

- [ ] README 开头明确写“single-participant research prototype”，不要写成生产级多租户 BI 平台。
- [ ] 明确区分两条路径：实时 VerbalVis 与 standalone `dataops_agent`；后者尚未接入实时链路。
- [ ] 检查 `.env`、API Key、录音、数据集授权和 trace 文件不会被提交；密钥只通过 `backend/.env` 或环境变量提供。
- [ ] 从干净环境按 README 执行安装、启动、健康检查和最小语音演示；前后端依赖与 DataOps 依赖分别说明。
- [ ] 验证后端：`pytest backend/tests -q`；验证前端：`npm test -- --run` 与 `npm run build`。如要发布 DataOps，再按 [PUBLISHING.md](PUBLISHING.md) 执行独立验收。
- [ ] 演示一次四种打断：附和、识别修正、停止、新分析意图；保留真实截图/GIF 之前不要伪造产品效果图。
- [ ] README 中写清指标口径，例如低分为 `review_score <= 2`、商品收入为不含运费的 `SUM(price)`。
- [ ] 写清事务保证范围：过期结果不发布；但同步 legacy handler 不提供 CPU 级中断。
- [ ] 在 Release note 中记录版本、commit SHA、复现环境、已知限制和变更；保留 [LICENSE](LICENSE)、[CONTRIBUTING.md](CONTRIBUTING.md) 与 [SECURITY.md](SECURITY.md) 链接。
- [ ] 不使用“高并发”“生产可用”“Redis 分布式状态已上线”“RAG 已接入”“多 Agent 协同”“真实 Docker 已验证”等超出当前证据的字眼。

### 3. 推荐的演示流程（2–3 分钟）

1. 打开初始仪表盘，说出一个明确问题，例如“比较不同州的低评分比例，并画柱状图”。
2. 让系统调用工具、生成图表并口头解释。指出工具不是自由生成 SQL，而是进入结构化工具和 DuckDB 的确定性路径。
3. 系统说话时说“嗯，继续”。前端应进入 overlap 提示后恢复原响应，说明“开口”并未直接取消。
4. 再说“等等，改成只看 2017 年的订单”。说明最终转写被判为分析修订；旧响应被 supersede，旧工具结果即使物理完成也不能提交。
5. 展示一次 `dashboard_commit`、`tool_batch_finished` 或 JSONL trace，解释 `response_id`、`intent_epoch`、`base_revision` 如何串起整个因果链。

### 4. 架构讲解图（适合 README 或答辩）

```text
Browser audio / text
        │ WebSocket
        ▼
FastAPI realtime adapter ────── Qwen-Omni-Realtime
        │                             │
        │  response transaction        │ function calls
        ▼                             ▼
ResponseCoordinator / TransactionManager
        │  private DashboardDraft
        ▼
schema-grounded tool registry ──► DuckDB / Olist data
        │
        ├─ fresh? conditional commit ──► Pinia state ──► Vega-Lite dashboard
        └─ trace events.jsonl / conversation.jsonl
```

### 5. 发布时的事实核验话术

**可以说：**“我实现了单会话下的响应事务、草稿执行和条件提交，以保证语义过期的工具结果不会写入仪表盘或回灌模型。”

**不要说：**“我已经实现了分布式事务、真正取消任意查询、生产级并发调度。”

**可以说：**“DataOps 模块提供 Redis Streams、知识检索和 MCP 的独立演进路径，未来可在明确的双重准入合同下接入。”

**不要说：**“VerbalVis 当前已经用 Redis/Elasticsearch/MCP 支撑实时运行。”

## 三、中文简历项目表述

### 1. Agent / LLM 工程岗位（推荐主版本）

**VerbalVis FD-Voice｜全双工语音驱动可视分析 Agent 原型**<br>
`Python · FastAPI · WebSocket · Qwen-Omni-Realtime · DuckDB · Vue 3 · Pinia · Vega-Lite`

- 面向 Olist 电商数据构建语音驱动可视分析原型，打通浏览器音频采集、Qwen-Omni-Realtime 对话/函数调用、DuckDB 聚合和共享仪表盘。
- 设计响应事务，以 `(response_id, intent_epoch, base_revision)` 绑定模型响应；**11 个**结构化分析工具先在私有 dashboard draft 中顺序执行，再通过条件提交发布，防止旧结果污染仪表盘与模型上下文。
- 实现“语义确认后再打断”：语音开始仅标记 overlap，最终转写后区分附和、识别修正、停止与分析修订；仅新的分析意图会替换当前响应并推进 epoch。
- 建立与模型注册 schema 共源的工具契约，声明读/草稿变更、依赖、前置条件、幂等性和不可抢占边界；补充 WebSocket 协议、JSONL trace 与前后端回归测试。

### 2. 数据 / 后端工程岗位（突出确定性分析与可靠性）

**VerbalVis FD-Voice｜可追踪的实时数据分析与仪表盘事务原型**<br>
`Python · FastAPI · DuckDB · WebSocket · Vue 3 · Vega-Lite`

- 基于 DuckDB 对 Olist 订单、商品、评价与配送数据实现受控聚合、筛选、比较和可视化生成；将低分比例、商品收入、配送时长等指标口径固化在后端工具中。
- 以私有状态快照复用既有同步工具处理器，工具批次执行失败或被新意图淘汰时恢复已提交状态；成功变更批次仅产生一次权威 `dashboard_commit` 与 revision 递增。
- 用 `response_id + intent_epoch + base_revision` 处理工具执行与用户语音输入的时序竞争，避免陈旧结果覆盖当前数据探索状态。
- 输出包含响应、工具、提交状态和丢弃原因的 JSONL trace，支撑问题定位、案例复盘与协议级自动化测试。

### 3. HCI / 可视分析 / 研究岗位（突出研究贡献）

**VerbalVis FD-Voice｜支持可恢复打断的全双工语音可视分析原型**<br>
`Qwen-Omni-Realtime · Vue 3 · Vega-Lite · Python · DuckDB`

- 探索全双工对话中“用户开口”与“用户修订分析意图”的差异；将 VAD 视为重叠信号，等待最终转写后再作可解释的四类打断决策。
- 将共享仪表盘更新组织为草稿执行、原子提交和单次 revision 递增，避免多工具分析过程中暴露半完成或语义过期的可视状态。
- 通过 `response_overlap`、`response_resumed`、`response_superseded` 等增量事件把响应生命周期呈现给用户，并保留 trace 支持交互过程复盘。
- 研究边界：当前为单参与者、内存态案例原型；不将形成性访谈或案例展示表述为已证明的用户效能提升。

### 4. 一页简历的极简版（可直接粘贴）

**VerbalVis FD-Voice｜全双工语音可视分析 Agent 原型**：以 FastAPI + Qwen-Omni-Realtime + DuckDB + Vue/Vega-Lite 实现实时语音分析；将 11 个 schema-grounded 工具纳入 `(response_id, intent_epoch, base_revision)` 响应事务，采用私有 dashboard draft 与条件提交隔离过期工具结果；实现附和/修正/停止/分析修订的打断分流、WebSocket 生命周期事件与 JSONL trace。

## 四、100 道高频面试问答

以下问题按面试出现概率和重要性排序。回答既可用于口述，也可作为 GitHub README、答辩讲稿和论文方法部分的事实核验清单。

### A. P0：项目故事、架构与核心机制（1–30）

#### 1. 这个项目要解决什么问题？

语音驱动数据分析的难点不只是把音频转成文本或调用一次 SQL。真实对话中，用户会在系统播报、规划或执行工具时插话；这句话可能是“嗯，继续”，也可能是“等一下，改看另一个范围”。VerbalVis 解决的是如何让这些打断不破坏已经进行的数据分析：短附和不误杀当前回答，真正的新意图又不会被旧工具结果覆盖。

#### 2. 你的项目一句话技术亮点是什么？

我把实时语音、模型函数调用、确定性数据工具和共享仪表盘放进一个可追踪的响应事务。每次响应由 `response_id`、`intent_epoch` 和 `base_revision` 标识；工具先执行在私有草稿，只有完成时仍属于当前用户意图和仪表盘基线，才会原子提交。

#### 3. 为什么不是普通的“语音 + LLM + 图表”Demo？

普通 Demo 往往只验证顺序流程：用户提问、模型调用工具、图表更新。但在全双工场景中，顺序会被重叠语音打破，模型、工具、UI 可能各自继续运行。项目额外定义了意图版本、响应所有权、草稿隔离、条件提交和前端事件语义，因此重点是运行时协调，而不是把几个 SDK 串起来。

#### 4. 为什么选择 Qwen-Omni-Realtime？

它适合把实时音频、对话输出和函数调用保持在同一条会话链路中，减少“ASR 服务—文本模型—TTS 服务”多段拼接的协调负担。模型负责自然语言与多模态交互，后端仍保有工具准入、指标计算和仪表盘提交的最终控制权；这避免了把业务事实和共享状态交给模型猜测。

#### 5. 整体架构是怎样的？

浏览器通过 WebSocket 发送音频/文本到 FastAPI realtime adapter，adapter 与 Qwen Realtime 保持会话。模型只能调用注册的分析工具，工具使用 DuckDB 查询 Olist 数据，并在私有 dashboard draft 上形成候选状态；TransactionManager 和 DashboardStore 判断是否新鲜后提交，再将权威快照同步给 Pinia/Vega-Lite。全过程将响应、工具与提交决定写入 JSONL trace。

#### 6. 为什么数据分析层使用 DuckDB？

Olist 是结构化、可本地复现的分析数据集，DuckDB 能嵌入 Python 进程，支持 SQL 聚合而不要求额外部署数据库服务器。对研究原型而言，它降低环境复杂度，同时让指标逻辑可以稳定、确定性地被工具调用。代价是它不等价于分布式数据仓库，所以项目不会宣称 PB 级或高并发查询能力。

#### 7. 为什么不让模型直接生成任意 SQL？

任意 SQL 会放大字段幻觉、指标口径漂移、无意的大查询和安全风险。当前方案把模型约束在 schema-grounded 工具：模型给出结构化参数，后端选择固定的数据字段、聚合逻辑和图表类型。这样既能保留自然语言交互，又能保证“低分”“收入”“延迟”等业务定义可审计、可测试。

#### 8. 11 个工具的价值是什么？

这些工具把分析行为分成范围更新、聚合比较、图表创建/更新/删除/高亮、仪表盘检查、总结和撤销等受控动作。它们不只是 function calling 的展示项，而是定义了模型对共享仪表盘能够产生什么影响、必须满足什么前置条件、能否幂等、是否可取消。工具数目有限也有利于完整测试其状态迁移。

#### 9. 什么是响应事务？

响应事务是从模型开始回答到工具结果被发布或丢弃的一段受控生命周期。事务持有响应身份、用户意图版本、起始 dashboard revision、当前状态、取消标记和拟议工具调用。它把“模型在说什么”“工具在算什么”“UI 应该展示什么”变成同一因果单元，而不是互不关联的异步回调。

#### 10. 为什么响应使用 `(response_id, intent_epoch, base_revision)` 三元组？

`response_id` 指向具体模型响应，`intent_epoch` 表示用户当前分析意图的版本，`base_revision` 表示工具开始时所基于的仪表盘版本。三者缺一不可：只有 response ID 无法区分同一会话中的意图更新；只有 epoch 无法发现外部 dashboard 已变化；只有 revision 也无法判断结果属于哪个模型响应。提交时同时检查它们，才能识别迟到或语义过期的结果。

#### 11. 为什么用户一开口不能直接取消回答？

语音活动只是时间上的重叠信号，不等价于意图改变。用户可能说“嗯”“好的”“继续”，也可能只是在修正识别文本；如果 VAD 一触发就取消，会频繁中断合作式对话。因此系统先进入 `OVERLAP_PENDING`，等待最终转写，在知道语义后才决定恢复、停止或替换响应。

#### 12. 系统怎样划分打断类型？

当前用一个保守、确定性的词法分类器划分 `BACKCHANNEL`、`RECOGNITION_REPAIR`、`STOP_ONLY` 与 `ANALYTICAL_REVISION`。附和和识别修正会恢复原响应，停止会终止当前响应但不凭空产生新分析目标，只有分析修订会 supersede 旧响应并推进 `intent_epoch`。规则化版本便于测试和解释，未来可在保持协议不变的条件下替换为置信度更高的分类器。

#### 13. `BACKCHANNEL` 为什么重要？

它代表“继续”“嗯”等对话协作信号，不应该被理解为新的分析任务。对它的正确处理是保留原 response、epoch 和播放资格，发送 `response_resumed` 让前端清除 overlap 提示。这个细节直接决定全双工体验是自然的还是“用户每发一声就把系统打断”。

#### 14. `RECOGNITION_REPAIR` 与新的分析意图有什么区别？

识别修正的目标是修复用户说法或转写，不必改变已有 dashboard 的分析语义，例如“抱歉，我说的是销量不是销量额”的澄清需要结合实际识别上下文审慎处理。当前保守分类将明确的修正语标为 repair，并维持原响应；真正带来筛选、指标或比较变化的请求才归为分析修订。这个区分避免了把每一句更正都当成强制重规划。

#### 15. `STOP_ONLY` 为什么不推进 `intent_epoch`？

“停一下”“不用了”只是撤销当前播报或执行，不代表用户已经提出新的分析目标。若它推进 epoch，会把“停止”错误等价为“切换到新任务”，为后续恢复或显式新问题制造不必要的版本跳变。因此系统取消响应但保持当前意图版本，直到用户给出明确的新分析请求。

#### 16. 什么情况下会发生 `ANALYTICAL_REVISION`？

用户明确修改了范围、指标、分组、比较对象或可视化需求时，例如“改成只看 2017 年”“不要按州，按品类比较”。此时旧工具批次的计算对象已不再正确，系统 supersede 当前响应、递增 epoch 并拒绝旧草稿提交。新问题随后作为新的规划/响应过程处理。

#### 17. 草稿执行（DashboardDraft）解决了什么问题？

如果每个工具直接写公开 dashboard，多工具批次执行到一半被打断时，用户会看到部分图表或不完整筛选状态。`DashboardDraft` 让工具先在私有快照中顺序执行；成功且新鲜时整体发布，失败、取消或过期时恢复已提交快照。它提供的是用户可见状态的原子性，而不是把所有旧处理器改写一遍。

#### 18. 条件提交具体校验什么？

提交会验证事务仍在允许提交的状态，未被取消，事务的 intent epoch 与当前 epoch 一致，事务和草稿的 base revision 与当前 dashboard 基线一致。写操作通过后才安装草稿并递增 revision；只读操作也要通过新鲜度校验，但不会伪造一次 dashboard 变更。任何检查失败都进入 `stale_discarded` 或失败路径。

#### 19. 什么是“过期结果不能污染状态”？

当用户已提出新问题时，旧工具即使返回了正确的数据，它相对于新意图也是错误的。系统不只是不把它画到图上，还不向浏览器发送该批次的工具卡/提交快照，也不把它作为 `function_call_output` 回灌给模型。这样旧事实不会影响 UI，也不会影响模型基于工具结果的下一轮推理。

#### 20. 旧的同步工具无法真正取消，系统如何保证正确性？

当前不声称能 CPU 级停止一个已在运行的 Python handler。运行时采用“逻辑取消”：handler 可以物理完成，但一旦 transaction 失去新鲜度，它的私有草稿和所有输出都会被丢弃。因此保证的是可观察正确性和状态一致性；资源浪费、查询取消和真正的抢占属于下一阶段需由可取消执行器/数据库驱动支持的问题。

#### 21. 为什么多工具调用要在同一个草稿中顺序执行？

一个分析请求常包含“先设范围、再聚合、再画图、再高亮”等有依赖的动作。顺序执行让后一个工具读取同一草稿中前一个工具的结果，并使成功后只需要一次全量提交。与并发写 dashboard 相比，它牺牲了单会话吞吐，但显著简化了依赖、回滚和用户可见一致性。

#### 22. 如何保证多工具调用只产生一次 revision 更新？

草稿内的各工具不触碰公开 revision；批次全部完成后，`DashboardStore.commit` 作为唯一发布点安装草稿。若该批次包含 mutation，成功提交只将 revision 加一并发送一次权威 `dashboard_commit`；若失败或 stale，revision 和公开快照都保持不变。这是“工具束原子发布”的可测试不变量。

#### 23. 读工具与写工具有什么区别？

`READ_ONLY` 工具只读取当前草稿/数据并产生解释性结果，不应改变 dashboard revision；`DRAFT_MUTATION` 工具可以在草稿中改变范围、视图或高亮，只有提交成功后才影响公开仪表盘。二者仍要经过相同的新鲜度检查，因为一个迟到的读结果回灌模型同样会破坏后续推理。

#### 24. 工具契约里除了 schema 还有什么？

每个 `ToolContract` 还声明模式、依赖、前置条件、幂等性、可取消性和效果说明。schema 回答“参数长什么样”，其他字段回答“工具能否在当前状态运行、是否会改状态、重试会不会重复副作用、能否抢占、产生何种影响”。这些元数据为准入、审计和后续调度提供了比函数签名更完整的语义。

#### 25. 为什么工具 schema 要与 Qwen 注册 schema 共源？

如果模型看到一份 schema、后端校验另一份 schema，字段和必填项会在维护中漂移。项目从已注册的 `TOOL_SCHEMAS` 生成不可变契约，并校验工具集合一致；模型能调用的接口和运行时允许的接口由同一事实源派生。这比“复制一份 JSON 再人工同步”可靠得多。

#### 26. 为什么用不可变工具契约？

工具调用发生在异步、可中断的链路中。如果运行中可随意改 schema 或权限元数据，就无法判断某次 trace 对应哪套规则。不可变合同使每个响应在其生命周期内面对稳定的输入定义和副作用声明，也让测试可验证 schema 与工具集合没有静默漂移。

#### 27. response ownership 是什么？

response ownership 是“哪个响应有资格提交工具结果和驱动 UI”的规则。当前活跃 response 由 transaction manager 管理；旧 response 被 supersede 或取消后，后续工具准入会失败。它防止异步回调按照到达顺序而非语义顺序覆盖状态，是全双工 Agent 的基本安全线。

#### 28. 前端如何表现这些状态，而不是只依赖后端？

后端通过增量 WebSocket 事件发送 overlap、恢复、supersede、取消、工具开始/结束、提交等状态。前端保存 `relevantResponseId`、`overlapPending`、`responseStatus`、`lastCommitStatus` 和丢弃原因；收到明确非 `committed` 的 dashboard 提交事件时不更新可视状态。这样浏览器既能反馈“系统正在处理什么”，也能抵御迟到消息。

#### 29. 为什么 `dashboard_commit` 要是权威快照？

浏览器增量地自行拼装工具副作用会受网络乱序、遗漏和局部状态差异影响。后端在条件提交后发送包含 views、filters/state 与 dashboard revision 的权威快照，前端以它替换本地公开状态。权威快照将最终决定权集中在一个明确的提交点，便于重连、调试和一致性验证。

#### 30. 这个设计和常见 RAG Agent 有什么差异？

RAG Agent 的重点通常是检索正确上下文；VerbalVis 的核心是实时交互中的“动作何时仍有效”。它同样有模型、工具和上下文，但最关键的是 response ownership、意图版本、草稿状态和条件提交。未来检索可以作为一个工具加入，但不能替代对共享 dashboard 状态的事务化协调。

### B. P1：实现细节、可靠性与验证（31–70）

#### 31. ResponseTransactionManager 的职责是什么？

它创建并维护响应的生命周期，记录当前活跃响应、当前 intent epoch、base revision、取消状态和 overlap 的返回状态。在新请求、最终转写、工具执行、提交、失败等节点推进状态，并给 DashboardStore 和工具准入提供“此响应现在是否仍有资格”的权威判断。

#### 32. overlap 的生命周期如何工作？

当检测到 `speech_started`，运行时把当前响应从流式/草稿执行的状态置为 `OVERLAP_PENDING`，但不立即清空模型输入或取消任务。最终转写到来后执行分类：附和/修正恢复先前状态，停止取消，分析修订 supersede 并推进 epoch。这个两阶段策略把时序信号和语义决策明确分离。

#### 33. 如果工具在等待最终转写期间执行完怎么办？

运行时在有限时间内等待 overlap resolution，避免因为用户短暂附和而把一个本应继续的结果误判为 stale。若最终分类为 backchannel 或 repair，则恢复原执行资格并允许条件提交；若是停止或分析修订，工具批次会被取消或视为过期。等待有边界，避免因转写缺失无限占用会话。

#### 34. 为什么 `response_superseded` 只停止指定 response 的播放？

全双工情况下新 response 可能已经开始生成。如果前端收到一个旧 response 的 supersede 就全局停止音频，会误伤新回答。前端按 response ID 做精确归属：只停止被替代的播放/反馈，并保留当前相关 response 的生命周期，从而抵御迟到事件。

#### 35. Qwen 的 function call 输出为什么要先于 `response.create`？

模型需要先看到每个 function call 的执行结果，才能在后续 response 中进行解释、继续规划或结束任务。如果先创建新的 response，模型可能在缺少工具事实的情况下开始生成。当前批次按工具调用顺序发送 `function_call_output`，确认事务成功后再发送一次 `response.create`。

#### 36. 为什么 stale 批次不能把 function output 回灌模型？

工具结果即使数据本身正确，也可能针对已被用户修改的范围。把它回灌会让模型以旧事实生成新回答，造成难以察觉的上下文污染。因此 stale 批次不仅不提交 UI，也不发浏览器工具结果和 Qwen function output；它是完整的语义隔离，而不是仅做 UI 过滤。

#### 37. `RLock` 快照桥接如何复用 legacy tools？

既有工具处理器依赖模块级 dashboard 全局状态。桥接层在 `RLock` 保护下捕获已提交快照，临时安装私有 draft，调用 legacy handler（关闭其公开 revision 自增），取得处理后的草稿后在 `finally` 恢复公开快照。通过此适配避免一次性重写所有工具，同时避免中间状态外泄。

#### 38. `RLock` 的代价和局限是什么？

它序列化了这段快照交换与同步 handler 执行，优先保证单会话正确性而非吞吐。它不能把进程内全局状态变成多用户隔离状态，也无法抢占已运行的阻塞函数；多会话版本需将状态按 session 隔离并设计数据库/任务执行器级的版本与取消语义。

#### 39. 为什么不是每个工具内部自己做权限、校验和审计？

把横切逻辑散落在 11 个工具里会造成规则不一致和遗漏。工具契约与 lifecycle hook 的思想是让 `before_tool_call` 集中校验参数、新鲜度、前置条件和响应所有权，在统一执行后记录延迟、结果大小、成功/失败和提交状态。当前项目已实现合同绑定和事务准入，hook 化是保持扩展性的自然接口。

#### 40. 你的工具参数校验做了哪些层次？

第一层是 Qwen 能看到的 JSON schema；第二层是后端对该 schema 的绑定与工具集合一致性检查；第三层是 handler 运行前的参数规范化、状态前置条件和 response ownership 检查；最后是提交时对 intent/version 的再校验。多层校验承认模型输出并不可信，并将“输入合法”和“现在仍可执行/可提交”分开处理。

#### 41. 为什么有 `base_revision` 还需要 `intent_epoch`？

dashboard revision 表示共享可视状态是否变化，但用户可以提出新的意图而尚未有任何 dashboard 提交。此时 revision 可能不变，旧工具却已语义过期；intent epoch 专门捕捉这种“任务目标变了而状态还没变”的情形。反过来，epoch 相同也不保证 dashboard 没被其他已提交操作修改，所以还需 revision。

#### 42. 为什么有 `intent_epoch` 还需要 response ID？

同一意图下可能存在模型重新生成、错误恢复或多段响应。response ID 将音频播放、模型事件、工具批次和前端反馈精确绑定到一个响应实体；它使系统能做到“只停止旧 response”，而不是把整个 session 粗暴地停止。epoch 表示语义版本，response ID 表示执行实例，两者不可互换。

#### 43. 工具失败时怎样避免半修改仪表盘？

mutation 都只影响私有草稿；工具束中任何工具失败时事务进入失败路径，不调用 DashboardStore 的公开 commit。`finally` 会恢复旧的已提交快照，所以 revision、views 和 filters 保持不变。前端也只有收到 `commit_status: committed` 的权威提交才更新仪表盘。

#### 44. 读工具为什么也需 freshness check？

读操作不改变 dashboard，但其结果可能会作为 function output 影响模型的下一轮回答，或者显示在浏览器工具卡中。若它属于已经 supersede 的响应，继续发布同样会误导用户与模型。因此“是否改 UI”与“是否仍可观察/回灌”是两件事；两类工具都需要语义新鲜度检查。

#### 45. 什么是 `stale_discarded`，与 `failed` 有何区别？

`failed` 表示工具或运行时未能完成，例如参数、查询或模型链路发生错误；`stale_discarded` 表示计算可能成功，但在提交前发现其 response/epoch/revision 已不再有效。区分二者很重要：前者是能力或可靠性问题，后者往往是用户及时改变意图后的正常竞争结果，应该在 trace 和指标中分别统计。

#### 46. 为什么要把最终事务状态写在 WebSocket 发送之前？

网络 `await send` 可能很慢或失败，如果内部 transaction 要等发送完成才标记 committed，其他事件可能仍把它当成执行中并发生错误竞争。先在后端原子地最终化状态，再发送外部通知，保证内部真相不依赖网络时序；发送失败可以单独作为交付问题处理，而不是回滚已经完成的事实。

#### 47. 前端如何抵御迟到的 dashboard 事件？

前端解析 `commit_status`；若该字段明确存在且不是 `committed`，就不应用 views、filters 或 revision，只记录状态/丢弃原因。再结合 response ID 的精确播放控制，旧事件到达也不会覆盖当前视图。后端是第一道权威门，前端这层是防御性协议执行。

#### 48. 为什么工具执行期间仍允许麦克风输入？

工具执行是系统内部工作，不应迫使用户等待或关闭表达通道。前端不因为 tool-running 状态屏蔽音频发送；用户可继续说话，后端以 overlap 与最终转写决定是否使在途工具失效。这是全双工体验的关键，但也要求后端实现草稿隔离，否则会放大竞态错误。

#### 49. 语音开始时为什么不清除 Qwen 输入缓冲？

在不知道用户真正意图前清除输入会丢失有效音频或误中断模型。系统先保留会话与输入，等最终转写明确归类后再按停止/修订语义处理。该策略把取消从低置信度的声学信号延迟到可解释的文本决策。

#### 50. 当前打断分类器为什么不用 LLM？

当前使用小而确定的规则，原因是它直接处于生命周期控制路径：可预测、低开销、便于用边界样本回归测试。规则覆盖不可能无限，未来可以引入 LLM/分类模型并输出置信度，但应将其作为可替换组件，保留“不确定时采取保守策略”和可审计决策记录。

#### 51. 如何处理 429、超时或模型错误？

实时链路将模型/工具错误转为明确终态，避免失败后仍提交草稿；响应状态、错误和 commit status 会记录在 trace 中。仓库不应宣称已有生产级重试、跨供应商 fallback 或 SLA；若扩展到服务化版本，应为 provider 调用增加指数退避、deadline、可重试分类和幂等结果处理，并将其纳入 transaction 合同。

#### 52. 你如何定义指标口径并保证一致？

模型不直接猜业务定义，而由确定性工具与 README 固定口径：低分为 `review_score <= 2`，商品收入为不含 freight 的 `SUM(price)`，类别服务指标以 `order_id + product_category` 为粒度以避免订单内重复商品的不当加权，延迟交付以实际日期晚于预计日期定义。图表和自然语言解释都应基于同一工具结果。

#### 53. 为什么把可视化状态视为 Agent context 的一部分？

用户的当前筛选范围、已有图表和高亮对象决定“这个”“再比较一下”“保留刚才的图”具体指什么。把它仅放在提示词摘要或模型记忆中会有误差；系统将 dashboard state 作为确定性状态源，通过权威快照与 revision 管理。模型可以询问/操作该状态，但不能以生成文本替代它。

#### 54. 系统如何处理撤销（undo）？

撤销属于受控工具和 dashboard 状态的一部分，而非让模型在文本中承诺“已撤销”。它与其他状态操作一样受到草稿、response ownership 和条件提交约束；若响应在撤销提交前过期，已提交 dashboard 不应发生改变。单会话内存态 undo 历史也是当前不支持多用户共享的原因之一。

#### 55. Trace 记录哪些关键信息？

事件 trace 会携带 response ID、intent epoch、base revision、工具元数据、时间信息、最终 commit status 与 discard reason。典型事件包括 `response_overlap`、`response_overlap_resolved`、`tool_batch_started`、`tool_result_staged`、`tool_batch_stale_discarded` 和 `tool_batch_finished`。这些信息足以重建一次响应为何成功、失败或被语义淘汰。

#### 56. Trace 能证明什么，不能证明什么？

它能证明运行时机制是否按合同执行，例如用户何时插话、分类如何做、哪些工具进入草稿、是否被提交或丢弃。它不能自动证明用户效率、工作负荷、信任感或 insight quality 提升；这些需要独立的受控用户研究。日志是机制可观测性的证据，不是人因有效性的替代品。

#### 57. 你会用哪些指标监控运行时？

从 trace 可以派生响应完成率、工具失败率、`stale_discarded` 比率、不同打断类型占比、平均每响应工具数、事务持续时间和配置错误数。若未来接入性能监控，再报告 VAD/转写、工具、首音频和端到端延迟的 P50/P95，并注明负载环境。当前仓库没有这些生产 SLA 数字，因此不能虚构具体毫秒数或成功率。

#### 58. 如何测试复杂时序竞态？

测试不只断言最终 UI 文案，而是构造可控阻塞的草稿执行，在执行中注入 speech overlap 或新的分析意图，再验证旧批次没有 `dashboard_commit`、revision 不增长、结果不回灌 Qwen。还覆盖 backchannel 恢复、`STOP_ONLY` 不推进 epoch、base revision 不匹配、迟到 overlap resolution 和多工具一次提交等协议不变量。

#### 59. 前端测试在这里的价值是什么？

后端判断正确不代表用户看见的行为正确。前端测试验证 overlap pending 时不会停止播放、恢复后反馈被清除、supersede 只停止指定 response、明确 stale 的 commit 不修改 dashboard，以及工具运行时不阻塞麦克风采集。这让 WebSocket 合同真正落到可见、可听的交互行为。

#### 60. 当前测试覆盖的不足有哪些？

现有重点是确定性状态机、协议和回归逻辑，不等同于真实 Qwen 网络、长期音频流、浏览器兼容性、网络乱序、负载、权限和生产故障恢复的验证。后续应补 provider sandbox 端到端测试、网络抖动/乱序注入、多会话并发、性能基准和人工任务研究，明确区分“合同测试通过”和“生产验证完成”。

#### 61. 你如何保证语音打断不会把过期分析写进仪表盘？

每个模型响应都绑定 `(response_id, intent_epoch, base_revision)`，工具只先写私有 `DashboardDraft`。提交前同时检查 epoch、基础 revision、事务状态和取消标记；任一不匹配就标记为 `stale_discarded`。此时不发布仪表盘、不发送浏览器工具结果、也不回灌 `function_call_output`，因此旧结果不会污染后续推理。

#### 62. 为什么不能把 `speech_started` 直接当作取消信号？

它只说明用户与系统在时间上重叠，不说明用户改变了分析任务。系统等待最终转写，区分附和、修正、停止和分析修订；只有最后一类才推进 epoch 并使在途草案失效。这样既保留自然的“嗯，继续”，也不放任新意图与旧工具竞争。

#### 63. 你的核心一致性不变量是什么？

第一，响应身份、意图版本和起始 dashboard 版本在响应生命周期内不可变；第二，只有仍然新鲜的事务可以提交；第三，成功 mutation 批次只产生一次 revision 增长和一次权威 `dashboard_commit`；第四，显式 stale/failed 批次不能改变公开 dashboard 或模型上下文。设计和测试都围绕这些可检查的不变量，而不是围绕某个特定 UI 截图。

#### 64. 为什么“旧处理器物理完成但结果不可见”仍有价值？

用户最重要的正确性是：眼前看到的图、听到的解释和模型用于重规划的事实必须属于当前意图。虽然不可抢占 handler 仍可能耗费资源，但条件提交把错误影响限制在私有草稿内。它是一个明确、诚实的过渡方案；要进一步节省资源，必须支持可取消查询或将工具迁移至可取消任务执行器。

#### 65. 如何验证提交和 WebSocket 发送之间没有竞态？

运行时先在内部将 transaction 状态最终化，再 `await` 向浏览器发送提交事件。测试覆盖该顺序，避免网络发送慢或失败时，其他事件仍把已完成事务误判为执行中。内部一致性与外部交付状态分离，才能清楚处理后续重连或发送失败。

#### 66. 为什么使用 snapshot 而不是彻底重写工具？

现有工具依赖模块级同步 dashboard 状态，全部重写虽然更理想但风险高、周期长。快照桥接用 `RLock` 临时安装私有状态、运行原 handler、捕获结果并在 `finally` 恢复已提交状态，实现了渐进式迁移。其限制也被记录：它适合单会话原型，不是多租户并发执行架构。

#### 67. 如何验证一个多工具动作只提交一次？

测试将初始 revision 固定为某个值，执行包含多个 mutation 的同一草稿批次，再断言成功后 revision 只加一、只出现一次 `dashboard_commit`。同样测试 stale、失败和 base revision 不匹配的情况，断言 revision 与公开快照不变。这样验证的是事务边界，而不仅是工具函数各自返回了成功。

#### 68. schema 共源设计如何防止接口漂移？

模型注册 schema 是唯一输入定义，`tool_contracts.py` 从它物化不可变合同，并对名称集合与 schema 对齐做校验。新增/删除模型工具而未提供对应契约会立即暴露，而不会等到模型在运行时调用不存在字段。它把静态接口维护前移到启动/测试阶段。

#### 69. 如何处理迟到事件？

事件和播放状态都由 response ID 归属，过期 response 无法重新获得工具准入。前端收到 supersede 仅停止目标 response，不对整个会话“一刀切”；显式 stale commit 不会更新 snapshot。测试还覆盖迟到的 overlap resolution 不影响 replacement response，防止典型的异步时序回归。

#### 70. 如果模型调用无效参数怎么办？

先由 JSON schema 限制参数形状，再由后端作规范化、前置条件和合同检查；非法参数不会直接触碰公开 dashboard。若执行阶段仍失败，整个草稿批次不提交，并将失败状态写入 trace。多层防线的目标不是假设模型永远正确，而是使其错误可控、可观察、可恢复。

### C. P2：研究、发布、安全与未来工作（71–100）

#### 71. 项目怎样评价“是否有效”？

应分三层：第一层验证机制正确性，例如过期结果不能提交、工具输出顺序与 revision 不变量；第二层做端到端案例，验证用户可完成范围修订、跨指标比较和图表操作；第三层才是受控人因研究，比较任务时间、错误率、工作负荷、感知控制和分析质量。当前强证据在前两层，不能将其扩展为普遍用户效能结论。

#### 72. 现有研究证据的强度如何表述？

形成性访谈用于发现需求、边界案例和设计要求，其价值在于 design-oriented insight，而非统计显著性。案例展示能说明机制在具体场景如何运作，但不估计总体人群上的可用性或绩效提升。面试中最稳妥的表述是“形成性设计探究加机制导向案例”，而不是“大规模用户实验验证”。

#### 73. 论文层面的核心贡献是什么？

第一，提出在全双工语音可视分析中区分“语音重叠”与“分析意图修订”的交互框架。第二，展示响应有效性、工具准入和共享 dashboard 状态如何通过事务协调。第三，用响应生命周期事件和案例说明语音适合快速转向，可视化适合持续比较与核验；贡献不在于提出新的基础语音模型。

#### 74. 为什么语音与可视化要组合，而不是只做语音助手？

语音降低表达追问、条件和方向变化的输入摩擦，图表则提供外部记忆、持久比较和对模型解释的核验。用户可以口头改变问题，同时保留已有图表作为分析上下文。仪表盘因此不是一次性回答的附件，而是会话中需要版本控制的共享状态。

#### 75. 你如何保证指标的语义正确？

指标由后端工具与文档的确定义共同约束，例如低评分为 `review_score <= 2`、商品收入只累计 `price` 而不含运费、延迟为实际交付日晚于预计交付日。对于类别服务指标，明确聚合粒度以避免订单内多商品重复加权。用户自然语言中的“收入”“差评”“延迟”最终都要落到这些可审计口径。

#### 76. 为什么 schema-grounded tools 比任意图表代码生成更适合这里？

它限制模型在已知字段、指标和 line/bar/scatter 等受支持的视觉操作内，服务器对结构化参数再规范化和校验。这样缩小了幻觉、越权与不可复现实验的空间，也使每个分析动作可以被测试和 trace。代价是表达自由度有限，但对研究原型和可靠的演示，这一取舍是合理的。

#### 77. 前后端如何共同抵御陈旧状态？

后端是状态权威，通过条件提交发布包含 `views`、状态和 `dashboard_revision` 的完整快照；前端对显式 `commit_status != committed` 的事件拒绝应用快照，并保留丢弃原因。两层防线避免网络迟到、前端状态异常或协议误用时将旧图覆盖新图。

#### 78. 当前最重要的性能边界是什么？

仓库没有端到端延迟、吞吐或并发压测数字，所以不能声称低延迟或高并发。已经实现的是避免无意义取消、在草稿中隔离工具和语义失效结果；但同步 handler 仍可能运行到结束。未来需要分段测量 VAD、转写、分类、工具、提交、首音频和端到端延迟，并在明确负载条件下报告分位数。

#### 79. 草稿事务的并发代价是什么？

`RLock` 和同步 legacy handler 让当前设计优先单会话正确性而非并行吞吐。它避免共享全局状态在同一响应中被半成品污染，却也意味着不能直接横向扩展为多个用户的并行工作器。生产化要先按 session 隔离状态，随后再引入可取消查询、并发控制和持久化恢复。

#### 80. 你会把当前系统直接部署到生产吗？

不会。当前定位是单参与者、内存态研究原型；生产至少还要完成认证授权、会话隔离、持久状态、错误恢复、监控告警、密钥管理、容量/安全测试与真实部署演练。发布 GitHub 的“可复现”与生产服务的“可运维”是两个不同层级，必须分开表述。

#### 81. GitHub 发布前会执行什么验证？

至少运行后端 `pytest backend/tests -q`、前端 `npm test -- --run` 和 `npm run build`，再从干净环境验证 README 的启动说明、配置错误的安全降级和一个真实最小演示。发布版本还应记录依赖版本、commit SHA、已知限制和数据/密钥边界。仓库没有自动 CI 的证据时，不能写成“CI 已全绿”。

#### 82. 如何管理 Qwen API Key 和配置错误？

密钥只从后端环境变量或 `backend/.env` 读取，绝不写进前端或源码。缺少 key 时，初始仪表盘仍可显示，后端发出一次 `configuration_error`，前端禁用麦克风但保持连接，避免无意义重连风暴；健康检查只报告是否配置，不回显密钥。发布前还需要检查 `.gitignore` 和日志不会记录请求头/凭据。

#### 83. 语音和 trace 日志的隐私风险是什么？

转写、事件、筛选范围和仪表盘状态可能暴露用户问题、业务偏好或数据范围，不能把它们当作无害调试文本。最低措施是字段最小化、保留期限、访问控制、脱敏流程和研究参与者知情同意；当前仓库的 trace 设计不等于已经取得完整合规认证，面试时应如实区分。

#### 84. DataOps Agent 是否已经是 VerbalVis 的后端？

不是。它是同仓库的独立服务包，包含数据接入、质量审计、受控 SQL、知识检索与 MCP 的演进路径；集成边界文档明确它没有导入到 VerbalVis 的实时模块。当前实时响应仍由进程内 transaction manager 和 dashboard store 处理，不能把 DataOps 的 Redis/Elasticsearch 能力写成 VerbalVis 已有事实。

#### 85. 独立 DataOps 模块有哪些可借鉴的可靠性模式？

其写接口使用 `Idempotency-Key`：相同 key 且相同请求重放首次响应，相同 key 但不同请求返回 409；受控 SQL 只允许 allow-list 的 `SELECT/WITH`，并限制默认返回行数；trace 采用输入最小化和凭据脱敏。这些是独立模块的合同，可作为未来集成的参考，不自动赋予实时语音链路同等能力。

#### 86. 为什么不立刻把 Redis 接进 VerbalVis？

Redis 只有在解决明确的跨进程状态、会话协调、任务队列或缓存问题时才有价值。当前 VerbalVis 是单会话内存态原型，直接加入 Redis 会增加状态一致性和失效语义，却未必改善核心研究问题。未来若扩展多 worker，应先定义 StateStore 的原子 compare-and-set、失效和恢复合同，并与本地 response ownership 做双重准入。

#### 87. Elasticsearch / Hybrid RAG 在当前系统中的地位是什么？

它们属于独立 DataOps 的可选知识检索路径，适合查询数据字典、审计规则、工具说明或历史事件；不应用向量检索替代确定性的 dashboard 当前状态或明确 ID 查找。当前 VerbalVis 没有将 Hybrid RAG 接入实时链路，因此不能把它列为实时产品已完成特性。

#### 88. MCP 在这个仓库中的边界是什么？

独立 DataOps 提供 stdio、只读 MCP 工具，方便外部客户端读取 schema、质量与任务信息。它不自动启动模型、不保留 VerbalVis 实时会话，也不意味着 Qwen 能通过 MCP 直接修改 dashboard。若未来接入，MCP 工具同样要接受 response transaction 的准入与条件提交规则。

#### 89. 当前系统如何降低模型不可靠性？

模型不直接执行任意代码，只能调用注册工具；参数需经过 schema、规范化、前置条件和合同检查；结果还需通过事务新鲜度判断才能公开。它降低了字段幻觉和状态污染，但无法保证模型的自然语言解释永远正确，因此用户仍应通过图表、指标定义和工具结果验证结论。

#### 90. 如何避免“日志很多但不能行动”的问题？

trace 不是堆原始文本，而是以 response ID、epoch 和 base revision 串联原因，明确记录 `committed`、`failed`、`stale_discarded` 与 discard reason。这样可把异常归因到分类、工具、版本冲突或交付环节，并计算事务丢弃率、工具失败率等未来指标。日志的目标是可复现诊断，而不是以采集更多敏感音频换取表面可观测性。

#### 91. 前端测试为什么不能省？

全双工语义最终由用户在浏览器中感知。前端需要验证 overlap 时继续播放、恢复后清除提示、supersede 不误伤新 response、stale commit 不改仪表盘、工具执行不阻塞麦克风。如果只测 Python 状态机，很可能出现协议正确但用户仍被错误停止或看到旧图的情况。

#### 92. 现有测试还缺什么？

缺真实 provider 沙箱端到端测试、长时音频、不同浏览器、网络断连/乱序注入、多会话并发、性能基准、权限攻击面和安全扫描。单元及合同测试可以证明设计不变量，但不能取代真实服务环境中的可靠性与人因验证。后续应把这些风险列入发布后的 issue/roadmap，而不是隐藏。

#### 93. 论文和实现不一致时怎么办？

必须主动同步。当前 `main.tex` 中部分叙述仍是较早的“speech onset 立即取消、工具输入关闭”的版本；README、运行时合同和测试描述的是“最终转写分类、私有草稿与条件提交”的新实现。投稿前应以当前可测试合同更新方法、图示、限制和案例，或冻结代码与文稿到同一版本，不能把两套保证混为一谈。

#### 94. 该研究的主要局限有哪些？

数据只来自 Olist 电商数据集，系统是单活跃会话、内存状态，形成性样本和案例不能代表不同专业领域或高风险任务。同步 legacy handler 也不保证资源级取消。把这些局限公开写出不会削弱贡献，反而说明我清楚机制证据、用户证据与生产证据之间的边界。

#### 95. 下一步最优先的技术工作是什么？

第一优先级是同步论文与实现；第二是从模块级全局状态迁移到 session 隔离的 dashboard store，并为查询提供真正可取消或有 deadline 的执行接口；第三是做故障注入、端到端验证、性能基准和部署观测。只有这些基础稳定后，才值得把独立 DataOps 的持久状态、审计和检索能力以明确合同接入。

#### 96. 下一步人因研究应怎样设计？

应设置全双工、半双工/文本、传统仪表盘等对照条件，预先定义任务和假设，测量完成时间、结果正确性、范围修订质量、工作负荷、感知控制和分析结论核验。还应记录实际打断类型，招募不同分析经验的参与者并覆盖多种任务复杂度。这样才能把“机制可行”推进到“对谁、在哪类任务上有益”。

#### 97. 用户说“继续”时系统具体做什么？

最终转写被识别为 `BACKCHANNEL` 后，系统恢复原 response 的执行/播放资格，保持原 epoch 与 dashboard 基线，并发出 `response_resumed`。它不会重启会话、不会重建工具批次，也不会产生新的 intent。这样的行为让用户能自然给出确认，而不需担心每句话都会打断分析。

#### 98. 如何保证失败不留下半修改的仪表盘？

所有 mutation 都先作用于私有草稿，只有 `DashboardStore.commit` 能发布公开状态。工具失败、用户停止、epoch 改变或 base revision 不匹配时，提交被拒绝，已提交 snapshot 和 revision 保持不变。前端也只应用明确 `committed` 的权威快照，形成后端原子性加前端防御性两层保障。

#### 99. 你如何定义“GitHub 发布就绪”？

对该原型而言，发布就绪意味着可从文档复现、配置不会泄密、后端测试和前端构建通过、关键事务 trace 可诊断、README 清楚说明启动与限制、论文/截图与实现没有矛盾。它不等于生产就绪；生产还需要认证、备份恢复、会话隔离、真实监控告警、负载测试和安全演练。

#### 100. 用一句话总结项目价值？

VerbalVis 的价值不是“用语音替代图表”，而是把全双工语音中的意图修订与模型响应、工具执行、共享仪表盘和上下文有效性协调为一套可追踪、可测试的事务机制；当前结论建立在机制和案例证据上，不夸大为用户绩效或规模化部署的结论。

---

## 附：面试最后 30 秒的回答模板

“我做 VerbalVis 时最关注的不是让模型多调用几个工具，而是用户随时插话后，旧工具结果还能不能影响当前分析。为此我给每次响应绑定 response ID、意图版本和 dashboard 基线；工具在私有草稿中执行，只有仍属于当前意图时才条件提交。这样‘嗯，继续’不会错误取消，而‘改成按品类看’会淘汰旧结果。我用 schema-grounded 工具和 DuckDB 保证数据口径，用 WebSocket 事件和 JSONL trace 证明状态机行为。当前它是单会话研究原型，我会明确说明尚未具备多租户、分布式状态和 CPU 级取消能力。”
